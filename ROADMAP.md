# ROADMAP — 邁向「全頻道可搜的存檔庫」

長期目標:把本站從「近期統計」長成**全頻道、可瀏覽、可全文搜尋的存檔庫**,
且**維持純靜態(GitHub Pages)、本機自管、不把頻道資料放外部服務**的核心原則。

關鍵前提(別忘):
- 資料是 `docs/manifest.js` + `docs/shards/YYYY-MM.js`(2026-09 起;之前是單一 `docs/data.js`),
  前端載完仍是全量進記憶體後 client-side 篩選。
- **真正的成本不是檔案大小,是「`data.js` 每小時都變 → 快取失效、整包重抓」。** 優化要讓「常變的部分小、不變的部分永久快取」。
- GitHub Pages 自動 brotli,純文字壓 ~5.5x:**實際下載量 ≈ 硬碟大小 ÷ 5**。
- 中文搜尋用「子字串包含」即可,**不需斷詞索引** —— 這讓暴力做法對中文堪用。
- ⚠️ 二進位檔 + 每小時更新會把 git 歷史撐爆(同「縮圖」「46k backfill」地雷)。任何大型/常變的二進位**不要每小時 commit 進 git**。

---

## ✅ 階段 1+2 已完成(2026-09-22)

回補到滿一年(15,946 則 / 2025-09-22 起)時,單一 `data.js` 來到 **9.0MB**,
GitHub Pages 實際送的是 **gzip 2.45MB(不是 brotli,÷3.7 不是 ÷5)**。
它是 render-blocking 的 classic script,在較慢的連線上整頁卡 60 秒以上,**等同打不開**。
這正是本文件設的 ~2MB 門檻要防的事,於是階段 1+2 一起做掉了。

**階段 1(瘦身)** —— `publish.py::trim()` 刪掉 `link`/`date_utc`/`iso_week`/
`week_range`/`preview.domain`,由 `app.js::rehydrate()` 推導回來。
實測省 **33.5%**(9.0MB → 6.43MB),高於原本估的 20–25%。
`node verify_rehydrate.js` 拿 `data/messages.json` 全量比對,15,946 則全部相符。

**階段 2(分片)** —— 每月一支 `docs/shards/YYYY-MM.js`,`docs/manifest.js` 列清單
並帶每片的內容雜湊。`app.js` 先載 manifest + 最新一片就開始渲染,其餘背景併發補。

實測(線上,較慢連線):
- 首屏阻塞 **2.45MB → ~150KB gzip**,manifest 525ms + 當月分片 1.1s,**2.3 秒可操作**
- 每小時的 commit 從「整包 data.js」變成 **只動 3 個檔**(manifest + 當月分片 + index.html)
- 舊分片網址帶內容雜湊 → 永久快取,回訪只需重抓當月那一片

仍未解的:**首次造訪的總量仍是 ~2.1MB**(只是不再阻塞)。真要再降,
就是「舊分片改成按需載入」——但「全部」範圍的排行與搜尋需要全量,
屬於階段 3 的範疇。

---

## 階段 0 — 現狀(2026-06,~2 個月 / 3099 則)
- 硬碟 `data.js` 1.76MB → **brotli 下載 ~0.32MB**。完全無壓力。
- **行動:不動。** 持續每小時增量;要更多歷史用 `FETCH_OLDER_DAYS=N python fetch.py` 往回補。

## 階段 1 — 瘦身欄位(便宜,~20-25% 折扣)
**觸發門檻:實際下載量接近 ~2MB(約累積一年資料)。**
不必獨立做 —— **建議併入階段 2 一起改**(兩者都動 `fetch.py` 輸出 + `app.js` 載入,分開做等於改兩次)。
- 從 `data.js` 拿掉可推導欄位,前端即時算回:
  - `link` ← `id` + 頻道 `link_base` 組
  - `date_utc`(前端未用)直接砍
  - `preview.domain` ← 從 `preview.url` parse
  - `iso_week` / `week_range` ← 用 `local_date` 算
    - ⚠️ **時區地雷**:前端算週次/日期**只能用本地欄位組字串,禁用 `toISOString()`**(在 +8 會倒退一天)。沿用 `app.js` 既有 `addDays()` 的做法。
- 欄位佔比參考(2026-06 量測):preview 45% / text 29% / link 7% / date_utc 6% / 週次相關 5% / 日期時間 5% / hashtags 3% / id 1%。

## 階段 2 — 按月分片 + 延遲載入(結構解法 ⭐)
**觸發門檻:同階段 1(~一年),或「全部」載入開始有感卡頓。**
- `fetch.py` 改輸出多檔:
  - `docs/shards/YYYY-MM.js`：舊月份**寫完即不變 → 瀏覽器/CDN 永久快取**。
  - `docs/data-latest.js`:只含當月,**只有它每小時變**(很小)。
  - `docs/manifest.js`:列出有哪些月份、各自則數與日期範圍。
- `app.js`:開頁先讀 manifest + 當月;使用者把範圍拉到更早 / 按「全部」時**才按需載入舊分片**(載過即快取)。
- 注意:「全部」的 hashtag 排行需跨所有分片 → 按需把全部分片載齊再算(或進階段 3 預算)。
- 完成後可順順撐到 **~10 萬則**(搜尋仍 < 0.1 秒,靠子字串掃描)。

## 階段 3 — 瀏覽器內 SQLite + HTTP Range(可無限擴張)
**觸發門檻:資料大到「搜全部要下載好幾 MB」開始痛(遠超 ~10 萬則 / 多年)。**
- `fetch.py` 端把訊息寫進 **SQLite**,建 **FTS5 `trigram`** 全文索引(trigram 正適合中文子字串搜)。Python 內建 `sqlite3` 即可。
- 前端改用 **`sql.js-httpvfs`**:透過 HTTP Range 只抓 DB 需要的頁 → 即使 DB 數百 MB,單次查詢只下載數十 KB。GitHub Pages 支援 Range。
- 最大改動是前端查詢層(記憶體陣列 → sql.js worker),約 1-2 天 + 測試。
- ⚠️ **拆兩層避免 git 爆肥**:龐大「歷史庫 `.db`」偶爾重建一次(或放 **GitHub Release asset**,不進 git 歷史);「當月新訊息」維持小 JS 每小時更新;前端兩邊一起搜。

## 不採用 — 真後端(Meilisearch / DB / serverless API)
搜尋體驗最強,但要養伺服器、把頻道資料放外部服務 —— **直接牴觸「純靜態、本機自管、不外放」核心原則**。除非放棄此前提,否則不做。

---

## 一句話路線
**先第一段(階段 1+2 一起做)、需要時再第二段(階段 3)。** 第一段的瘦身/分片/按需載入,在階段 3 仍是「當月那一層」的基礎,不會白做 —— 平滑接軌,不必為長期目標現在就做痛苦大改。
